UPDATE OR INSERT INTO FORMULARIO(FORM_CODIGO, FORM_NOME, FORM_TIPO, MDL_CODIGO)
               VALUES(97,'Cadastro de Conexão HeraTalk','F',1);

UPDATE OR INSERT INTO FORM_ACAO(FORM_CODIGO, ACAO_CODIGO)
               VALUES(97,1);
UPDATE OR INSERT INTO FORM_ACAO(FORM_CODIGO, ACAO_CODIGO)
               VALUES(97,2);
UPDATE OR INSERT INTO FORM_ACAO(FORM_CODIGO, ACAO_CODIGO)
               VALUES(97,3);
UPDATE OR INSERT INTO FORM_ACAO(FORM_CODIGO, ACAO_CODIGO)
               VALUES(97,4);

UPDATE OR INSERT INTO FORM_ACAO_PERFIL(FORM_CODIGO, ACAO_CODIGO,PERF_CODIGO)
               VALUES(97,1,1);
UPDATE OR INSERT INTO FORM_ACAO_PERFIL(FORM_CODIGO, ACAO_CODIGO,PERF_CODIGO)
               VALUES(97,2,1);
UPDATE OR INSERT INTO FORM_ACAO_PERFIL(FORM_CODIGO, ACAO_CODIGO,PERF_CODIGO)
               VALUES(97,3,1);
UPDATE OR INSERT INTO FORM_ACAO_PERFIL(FORM_CODIGO, ACAO_CODIGO,PERF_CODIGO)
               VALUES(97,4,1);

COMMIT;

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (40, 2, 'PERMITE MOV. SERV. ART. DATA RETROATIVA', '6', NULL, 0, 'F')
                       MATCHING (PAR_CODIGO);

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (41, 2, 'GERA COM. REP. FECH. COMISSAO', '6', NULL, 0, 'V')
                       MATCHING (PAR_CODIGO);

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (42, 2, 'TIPO DOC FECH. COMISSAO', '0', NULL, 0, '000007')
                       MATCHING (PAR_CODIGO);

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (43, 2, 'CENTRO CUSTO FECH. COMISSAO', '0', NULL, 0, '000013')
                       MATCHING (PAR_CODIGO);

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (44, 2, ' CODIGO OPERACAO FOLHA FECH. ARTESANAL', '0', NULL, 0, '000008');

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (45, 2, 'CODIGO SETOR FINANCEIRO', '0', NULL, 0, '000001');

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                         VALUES (46, 0, 'PATH DADOS NFe', '1', NULL, 10, '')
                       MATCHING (PAR_CODIGO);

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                VALUES(47, 0, 'URL API HERA TALK', 0, NULL, 0, 'HTTP://146.235.51.220:15071');

UPDATE OR INSERT INTO PARAMETRO (PAR_CODIGO, GP_CODIGO, PAR_DESCRICAO, PAR_TIPO_REGISTRO, PAR_MASCARA, PAR_USO, PAR_VALOR)
                VALUES(48, 0, 'CHAVE API HERA TALK', 0, NULL, 0, '');

COMMIT;

CREATE TABLE HT_CONEXAO (
    HTC_CODIGO        INTEGER NOT NULL,
    HTC_DESCRICAO     VARCHAR(60) NOT NULL,
    HTC_SITUACAO      VARCHAR(20),
    HTC_INSTANCIA     VARCHAR(20),
    HTC_JWT_KEY       VARCHAR(300),
    HTC_TELEFONE      VARCHAR(15),
    HTC_NOME_USUARIO  VARCHAR(60),
    HTC_URL_IMAGEM    VARCHAR(300),
    USR_CODIGO        INTEGER NOT NULL,
    HTC_DATA_QRCODE   TIMESTAMP,
    HTC_DATA          TIMESTAMP NOT NULL
);

ALTER TABLE HT_CONEXAO ADD CONSTRAINT PK_HT_CONEXAO PRIMARY KEY (HTC_CODIGO);
ALTER TABLE HT_CONEXAO ADD CONSTRAINT FK_HTC_USUARIO FOREIGN KEY (USR_CODIGO) REFERENCES USUARIO (USR_CODIGO);

CREATE SEQUENCE GEN_HT_CONEXAO;
ALTER SEQUENCE GEN_HT_CONEXAO RESTART WITH 1;

COMMIT;

ALTER TABLE FUNCIONARIO ADD FUN_CHAVE_HERA_TALK VARCHAR(40);
ALTER TABLE FUNCIONARIO ADD FUN_TIPO_CHAVE_PIX VARCHAR(1);
ALTER TABLE FUNCIONARIO ADD FUN_CHAVE_PIX VARCHAR(150);

ALTER TABLE EMPRESA ADD EMP_EMAIL_POP VARCHAR(60);
ALTER TABLE EMPRESA ADD EMP_NOME_POP VARCHAR(60);
ALTER TABLE EMPRESA ADD EMP_HOST_POP VARCHAR(60);
ALTER TABLE EMPRESA ADD EMP_AUTENTICACAO_POP VARCHAR(40);
ALTER TABLE EMPRESA ADD EMP_USUARIO_POP VARCHAR(40);
ALTER TABLE EMPRESA ADD EMP_SENHA_POP VARCHAR(100);
ALTER TABLE EMPRESA ADD EMP_USETLS_POP VARCHAR(40);
ALTER TABLE EMPRESA ADD EMP_METODOTLS_POP VARCHAR(40);
ALTER TABLE EMPRESA ADD EMP_PORTA_POP INTEGER;

ALTER TABLE MAQUINA ADD MAQ_INFO_TEC BLOB SUB_TYPE 1 SEGMENT SIZE 16384;

COMMIT;

UPDATE USUARIO SET USR_LOGIN = 'ADMINISTRADOR', USR_SENHA = '5225D81D1A60A6918FF2'
WHERE USR_CODIGO = 1;

ALTER TABLE DADOS_SISLAV_WISE ADD DSW_URL_HERAUPDATE VARCHAR(60);
UPDATE DADOS_SISLAV_WISE SET DSW_URL_HERAUPDATE = 'http://herasis.ddns.net:15099';

COMMIT;

CREATE TABLE PILOTO_ARTESANAL (
    PLT_CODIGO  INTEGER NOT NULL,
    ART_CODIGO  INTEGER NOT NULL
);

ALTER TABLE PILOTO_ARTESANAL ADD CONSTRAINT PK_PILOTO_ARTESANAL PRIMARY KEY (PLT_CODIGO, ART_CODIGO);
ALTER TABLE PILOTO_ARTESANAL ADD CONSTRAINT FK_PA_REL_ART FOREIGN KEY (ART_CODIGO) REFERENCES ARTESANAL (ART_CODIGO);
ALTER TABLE PILOTO_ARTESANAL ADD CONSTRAINT FK_PA_REL_PLT FOREIGN KEY (PLT_CODIGO) REFERENCES PILOTO (PLT_CODIGO);

COMMIT;

UPDATE Formulario SET Form_Nome = 'Relatório De Movimentações De Materiais' WHERE Form_Codigo = 87;
UPDATE Formulario SET Form_Nome = 'Relatório De Entrada De Materiais' WHERE Form_Codigo = 88;
UPDATE Formulario SET Form_Nome = 'Relatório De Estoque De Materiais' WHERE Form_Codigo = 59;
UPDATE Formulario SET Form_Nome = 'Relatório De Compra De Materiais' WHERE Form_Codigo = 61;
UPDATE Formulario SET Form_Nome = 'Relatório De Saída De Materiais' WHERE Form_Codigo = 89;

ALTER TABLE TIPO_DOCUMENTO ADD TD_CHAVE_PIX VARCHAR(30);

COMMIT;

